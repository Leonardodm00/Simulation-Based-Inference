__pycache__/
*.py[cod]
.ipynb_checkpoints/
.venv/
venv/

# job output
logs/*
!logs/.gitkeep
*.o[0-9]*
*.e[0-9]*

# data and trained models -- never commit these
*.parquet
*.npz
*.pt
*.ckpt
data/
runs/
# sbi writes tensorboard summaries here on every train() call
sbi-logs/
runs/

# hpc/dsn -- the mirrored Deep-Summary-Network tree (migration step 1,
# 2026-09-19). Same machine state the DSN repo ignored, now under this path.
dsn/out/
dsn/outputs/
dsn/trials.jsonl
dsn/search_state.json
dsn/config_best.json
dsn/config_top*.json
dsn/**/checkpoints/
dsn/**/figures/
dsn/**/config_*_lane[0-9]*.json
dsn/**/npz_specs_*.json
dsn/**/cache_*/
# Smoke_Tests writes these INTO THE TREE on every run (rasters, triplet plots,
# burst specs). They were never ignored, so a `git add -A` after a suite run
# commits ten binaries. Stage D, 2026-09-21.
dsn/Smoke_Tests/aug_debug/
dsn/Smoke_Tests/burst_data/
dsn/Smoke_Tests/pipeline_out/
